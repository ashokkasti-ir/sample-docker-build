[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sample-variation-python"
version = "0.1.0"
description = "Python fixture for workflow variation tests"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["samplepkg"]
